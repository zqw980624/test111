<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yami.trading.dao.item.TItemLeverageMapper">

	<sql id="tItemLeverageColumns">
		a.UUID AS "uuid",
		a.ITEM_ID AS "item.uuid",
		a.LEVER_RATE AS "leverRate",
		a.create_by AS "createBy",
		a.create_time AS "createTime",
		a.update_by AS "updateBy",
		a.update_time AS "updateTime",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag",

		item.name AS "item.name",
		item.symbol AS "item.symbol"
	</sql>

	<sql id="tItemLeverageJoins">
		left join t_item item on item.UUID = a.ITEM_ID
	</sql>



	<select id="findById" resultType="com.yami.trading.bean.item.dto.ItemLeverageDTO">
		SELECT
			<include refid="tItemLeverageColumns"/>
		FROM t_item_leverage a
		<include refid="tItemLeverageJoins"/>
		WHERE a.uuid = #{id} and a.del_flag = 0
	</select>

	<select id="findList" resultType="com.yami.trading.bean.item.dto.ItemLeverageDTO">
		SELECT
			<include refid="tItemLeverageColumns"/>
		FROM t_item_leverage a
		<include refid="tItemLeverageJoins"/>
	    ${ew.customSqlSegment}
	</select>


</mapper>
