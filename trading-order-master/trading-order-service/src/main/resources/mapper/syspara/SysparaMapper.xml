<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yami.trading.dao.syspara.SysparaMapper">

	<sql id="sysparaColumns">
		a.UUID AS "uuid",
		a.CODE AS "code",
		a.PARTY_ID AS "partyId",
		a.SVALUE AS "svalue",
		a.SORDER AS "sorder",
		a.STYPE AS "stype",
		a.NOTES AS "notes",
		a.MODIFY AS "modify"
	</sql>

	<sql id="sysparaJoins">

	</sql>


	<update id="updateBatch">
		<foreach collection="sysparaList" item="syspara" separator=";">
			UPDATE t_syspara SET svalue = #{syspara.svalue} WHERE code = #{syspara.code}
		</foreach>
	</update>

	<select id="findById" resultType="com.yami.trading.bean.syspara.dto.SysparaDTO">
		SELECT
			<include refid="sysparaColumns"/>
		FROM t_syspara a
		<include refid="sysparaJoins"/>
		WHERE a.uuid = #{id} and a.del_flag = 0
	</select>

	<select id="findList" resultType="com.yami.trading.bean.syspara.dto.SysparaDTO">
		SELECT
			<include refid="sysparaColumns"/>
		FROM t_syspara a
		<include refid="sysparaJoins"/>
	    ${ew.customSqlSegment}
	</select>


</mapper>
