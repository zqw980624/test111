<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yami.trading.dao.user.HighLevelAuthRecordMapper">

  <select id="listRecord" resultType="com.yami.trading.bean.user.dto.HighLevelAuthRecordDto">
      SELECT hl.*,u.user_name,u.user_code,u.role_name,
      ur.user_name AS 'recomUserName',
      ur.user_code AS 'recomUserCode' FROM tz_high_level_auth_record  hl
      LEFT JOIN  tz_user  u ON  hl.user_id=u.user_id
      LEFT JOIN tz_user ur ON u.user_recom = ur.user_id
           where 1=1 and  hl.del_flag=0
        <if test="rolename!=null and rolename!=''">
             and u.role_name=#{rolename}
        </if>

      <if test="status!=null and status!=''">
          and hl.status=#{status}
      </if>

      <if test="userName!=null and userName!=''">
          and u.user_name=#{userName} or u.user_code=#{userName}
      </if>


      order by hl.create_time desc
  </select>
</mapper>
