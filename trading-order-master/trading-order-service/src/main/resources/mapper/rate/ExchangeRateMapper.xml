<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yami.trading.dao.rate.ExchangeRateMapper">

	<sql id="exchangeRateColumns">
		a.UUID AS "uuid",
		a.OUT_OR_IN AS "outOrIn",
		a.RATA AS "rata",
		a.CURRENCY AS "currency",
		a.NAME AS "name",
		a.CURRENCY_SYMBOL AS "currencySymbol"
	</sql>

	<sql id="exchangeRateJoins">

	</sql>



	<select id="findById" resultType="com.yami.trading.bean.rate.dto.ExchangeRateDTO">
		SELECT
			<include refid="exchangeRateColumns"/>
		FROM t_exchange_rate a
		<include refid="exchangeRateJoins"/>
		WHERE a.uuid = #{id} and a.del_flag = 0
	</select>

	<select id="findList" resultType="com.yami.trading.bean.rate.dto.ExchangeRateDTO">
		SELECT
			<include refid="exchangeRateColumns"/>
		FROM t_exchange_rate a
		<include refid="exchangeRateJoins"/>
	    ${ew.customSqlSegment}
	</select>


</mapper>
